#include<iostream>
#include"golf.h"

using namespace std;

const int n = 20;

void main()
{
	golf arr[n];
	int i;
	for (i = 0; i < n; i++) {
		int out = setgolf(arr[i]);
		if (out == 0) break;
	}
	for (int j = 0; j < i; j++)
		showgolf(arr[j]);
}
