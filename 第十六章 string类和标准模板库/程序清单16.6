#include<iostream>
#include<string>
#include<memory>

using namespace std;

void main()
{
	shared_ptr<string> data[4]{
		auto_ptr<string>(new string("Fowl Balls")),
		auto_ptr<string>(new string("Duck Dalls")),
		auto_ptr<string>(new string("Chicken Runs")),
		auto_ptr<string>(new string("Turkey Errors")),
	};
	shared_ptr<string> win;
	win = data[2];
	for (int i = 0; i < 4; i++) {
		cout << *data[i] << endl;
	}
	cout << "The winer is: " << *win << endl;
}
